import UploadFile from "@/components/dashboard/UploadFile";

const page = () => {
  return (
    <main className="flex h-full min-h-0 w-full">
      <UploadFile />
    </main>
  );
};

export default page;
