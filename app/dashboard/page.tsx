import UploadFile from "@/components/dashboard/UploadFile";

const page = () => {
  return (
    <main className="col-span-2 flex h-full min-h-0 w-full">
      <UploadFile />
    </main>
  );
};

export default page;
